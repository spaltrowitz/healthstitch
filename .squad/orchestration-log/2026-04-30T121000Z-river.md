# Performance Optimizations: Indexes & Aggregates

**Agent:** River  
**Date:** 2026-04-30T121000Z  
**Duration:** 40 minutes (estimated completion)  

## Scope

Expression indexes on date columns, pre-computed training_load_aggregates table with incremental updates, trends API gap indicators.

## Deliverables

### Backend Migrations (`backend/src/migrations/`)
- [ ] `004_expression_indexes.sql` — expression indexes on date(recorded_at), date(start_at), composite (user_id, source, date)
- [ ] `005_training_load_aggregates.sql` — new table for weekly/monthly rollups

### Backend Services (`backend/src/services/`)
- [ ] `aggregateService.js` — updateAggregatesForWorkout, recomputeAll, pre-compiled statements
- [ ] Integration with `ingestService.js` — call updateAggregatesForWorkout after workout ingest
- [ ] Updated `dashboardRoutes.js` — trends endpoint now calls fillGaps, returns has_data field

### Data Model Updates
- [ ] Trends API response shape: each metric series includes `has_data` on every point
- [ ] Workouts `weekly_load` and `monthly_load` include `count`, `avg_strain`, `calories` alongside `load`

## Success Criteria

- Dashboard requests complete in <50 ms (vs prior ~200 ms on scan)
- Aggregates incremental (one workout = one row update, not recalculate all)
- Trends endpoint fills missing dates with has_data=false
- No regression on morning check-in or device comparison routes

## Dependencies

- Wash's data fixes (Phase 1) — baseline/sync_state tables must be in place
- Frontend update (Kaylee) — to consume has_data field

## Status

- **Not Started**
