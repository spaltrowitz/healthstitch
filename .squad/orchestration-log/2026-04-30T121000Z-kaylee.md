# Sync Status Freshness UI

**Agent:** Kaylee  
**Date:** 2026-04-30T121000Z  
**Duration:** 20 minutes (estimated completion)  

## Scope

Build SyncStatus component with green/amber/red status coding, auto-refresh 60s, graceful 404 handling, retry on error.

## Deliverables

### Frontend (`frontend/src/`)
- [ ] `components/SyncStatus.jsx` — component fetching /whoop/sync-status and /apple/sync-status
- [ ] Color coding: green (<30 min), amber (30 min–2 h), red (>2 h)
- [ ] Auto-refresh every 60s
- [ ] Graceful 404 handling (show "—" if endpoint missing)
- [ ] Retry button on error
- [ ] Integration in `App.jsx` — render between header and tabs

## Success Criteria

- Displays relative time since last sync (e.g., "5 min ago")
- Green/amber/red indicator matches staleness thresholds
- No crash on 404 or unexpected API response
- Auto-refresh interval runs without memory leak
- Compact visual style matches existing card design

## Dependencies

- Wash's Phase 1 (WHOOP /api/whoop/sync-status endpoint — already exists)
- Wash's Phase 2 (Apple /api/apple/sync-status endpoint)
- River's sync logic (populating sync_state tables)

## Status

- **Not Started**
