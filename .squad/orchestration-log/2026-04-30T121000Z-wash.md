# Apple Watch Continuous Sync — Phase 2

**Agent:** Wash  
**Date:** 2026-04-30T121000Z  
**Duration:** 30 minutes (estimated completion)  

## Scope

Implement HKObserverQuery observers, HKAnchoredObjectQuery for delta sync, background URLSession, Keychain JWT, BGAppRefreshTask fallback, GET /api/apple/sync-status endpoint.

## Deliverables

### iOS Companion (`ios-companion/HealthSyncCompanion/`)
- [ ] `BackgroundSyncManager.swift` — singleton for observer queries, anchored queries, background URLSession, BGAppRefreshTask
- [ ] `KeychainHelper.swift` — JWT storage with kSecAttrAccessibleAfterFirstUnlock
- [ ] AppDelegate integration — background task registration, observer startup
- [ ] `HealthSyncCompanionApp.swift` — AppDelegate wiring
- [ ] `ContentView.swift` — Keychain-backed JWT (migration from UserDefaults)
- [ ] `Info.plist` — UIBackgroundModes, BGTaskSchedulerPermittedIdentifiers

### Backend (`backend/src/`)
- [ ] `migrations/004_apple_sync_state_enhance.sql` — enhanced sync_state table
- [ ] `routes/appleRoutes.js` — GET /api/apple/sync-status endpoint
- [ ] Integration with ingest path — upsert to enhanced sync_state

## Success Criteria

- Background observers register without crash
- Anchored queries fetch delta correctly in background
- Background URLSession uploads survive app suspension
- JWT persists via Keychain across launches
- `/api/apple/sync-status` returns staleness_minutes and failure state
- Data freshness: Apple Watch HR/HRV ~1h, workouts ~15 min

## Dependencies

- Wash's prior Phase 1 work (backend routes, basic apple_sync_state table)
- Kaylee's SyncStatus component (will consume /api/apple/sync-status)
- River's performance work (expression indexes, aggregate tables)

## Status

- **Not Started**
