# Orchestration Log: Wash (2026-04-30T115000Z)

**Agent:** Wash (Backend Developer, claude-opus-4.6)  
**Session:** 2026-04-30T115000Z-data-fixes-and-sync  
**Duration:** ~2 hours (estimated from workload)

## Scope
Fixed all 6 data bugs identified by River's review.

## Completed Tasks

### Data Bug Fixes (6 total)

**Critical Issues:**
1. ✅ Per-source HRV baselines — WHOOP RMSSD and Apple SDNN now compared separately; morning check-in API response nests by source
2. ✅ WHOOP baseline computation — added baseline_90d for hrv_rmssd; WHOOP-only users now receive deltas

**Important Issues:**
3. ✅ Sleep date normalization — both sources now use start_at date as sleep_date ("night of" convention)
4. ✅ WHOOP token upsert UNIQUE constraint — recreated token table with UNIQUE(user_id) for correct ON CONFLICT behavior
5. ✅ Pre-compiled database statements — removed 7 inline db.prepare() calls from dashboard routes
6. ✅ Separated strain from active energy — exposed as distinct series (strain.whoop: 0–21 score, strain.apple_active_energy: kcal)

## Decisions Made

- Morning check-in `hrv` field changed to nested-by-source structure
- Trends `strain` field simplified: removed `apple_load` + `rolling_7d_load` confusion, now exposes `whoop` (0–21) and `apple_active_energy` (kcal) separately
- Future combined load index requires explicit design with documented normalization

## Cross-Team Impact

**Kaylee (Frontend):** Must update dashboard components to render per-source HRV/RHR and reflect new `strain` field structure.

## Open Items

- Timezone awareness (UTC midnight vs user local midnight) — deferred
- Unit standardization for energy metrics — addressed in this session

## Code Changes Summary

- Created/updated migrations for whoop_sync_state and token table schema
- Updated morning check-in endpoint to return nested hrv/resting_hr by source
- Updated trends endpoint strain field structure
- Updated dashboard queries to remove inline prepares

## Commit Ready

Changes staged. Ready for merge.
